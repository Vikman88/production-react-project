{
  "compilerOptions": {
    "outDir": "./dist/",
// Не позволяет использовать переменные где не указан тип
    "noImplicitAny": true,
    "module": "esNext",
// В какую спецификацию компилируем
    "target": "es5",
    "jsx": "react-jsx",
// Компилятор будет обрабатывать не только ts но и js файлы
    "allowJs": true,
    "moduleResolution": "node",
// позволяет работать с пакетами, которые используют common js как с обычными пакетами с помощью import (require() module.export) = common js
    "esModuleInterop": true,
// Позволяет исп export default
    "allowSyntheticDefaultImports": true,
// Для абсолютного импорта
    "baseUrl": ".",
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS",
    }
  }
}